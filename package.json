{
  "name": "jardilock-website",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --host 0.0.0.0 --port 3000 --strictPort",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "preview": "nuxt preview",
    "test:unit": "vitest run --exclude tests/smoke/**",
    "test:smoke": "playwright test"
  },
  "dependencies": {
    "@meteocons/svg": "^0.1.0",
    "@vuepic/vue-datepicker": "^12.1.0",
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3",
    "nuxt": "^3.15.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vitest": "^4.1.4"
  }
}
